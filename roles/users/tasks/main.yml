---
    - debug:
        msg: "{{ username }}"

    - name: New user add
      user:
        name:
          - "{{ username }}"
        password: "{{ 'list.password' | password_hash('sha512') }}"
        shell: "/bin/{{ 'list.shell' }}"
