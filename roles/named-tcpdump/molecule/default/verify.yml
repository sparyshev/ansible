---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: "Include named-tcpdump"
    include_role:
      name: "named-tcpdump"
#    assert:
#      that: true
